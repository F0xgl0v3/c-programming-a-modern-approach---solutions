#include <stdio.h>

// If i is an int variable and p and q are pointers to int, which of the following assignments are legal?

int main( void ) {

	printf( "a) - no\nb) - no\nc) - wrong yes - acually no because seemingly we cant store a pointer in an int\nd) - wrong yes - same reason as above\ne) - yes\nf) - yes\ng) - no\nh) - no\ni) - yes\n" );

	return 0;

}
