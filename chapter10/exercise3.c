#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <fcntl.h>

// Suppose that a program has only one function ( main ). How many different variables names i could this programm contain?

int main( void ) {

	printf( "Since we are limited to one function only and not one block only we can theoretically have an infinite number of variables named i\n" );
	return 0;

}
